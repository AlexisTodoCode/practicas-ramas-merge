# Configuraci�n de la base de datos
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/sales-system
    username: root
    password:
    hikari:
      connection-timeout: 20000
      maximum-pool-size: 10

  # Configuraci�n de JPA/Hibernate
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
